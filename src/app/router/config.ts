const routing = {
  home: '/',
  login: '/login',
  user: '/user/:id',
  form: '/test-form',
  refresh: '/refresh',
  appInstanceView: '/app-instance/:arn',
  deployLog: '/deploys/:deployId',
  deployList: '/deploys',
};

export default routing;
